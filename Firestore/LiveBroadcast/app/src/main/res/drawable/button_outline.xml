<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" />  <!-- 아웃라인 색상을 흰색(#FFFFFF)으로 설정 -->
    <stroke
        android:width="2dp"
        android:color="#000000" />  <!-- 아웃라인의 선 두께와 색상을 흰색(#FFFFFF)으로 설정 -->
    <corners android:radius="8dp" />  <!-- 버튼 모서리를 둥글게 설정 -->
</shape>